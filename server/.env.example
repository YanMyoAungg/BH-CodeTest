NODE_ENV=production

# Server port
PORT=3000

# JWT signing secret (required)
# Use separate secrets for access and refresh tokens in production:
# JWT_ACCESS_SECRET=...
# JWT_REFRESH_SECRET=...
# Or use a single fallback:
JWT_SECRET=JWT_SECRET_TOKEN
